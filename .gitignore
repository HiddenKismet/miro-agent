node_modules/
.pi-glla/
pi-session-*.html
*.log
.DS_Store

# Go build artifacts
miro-tui/miro-tui

# Miro core: independent Pi fork repository (see README — clone separately).
# It keeps its own git history and upstream remote; never tracked by this repo.
/core/
